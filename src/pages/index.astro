---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
// import { getCollection } from "astro:content";

// const posts = (await getCollection('blog')).sort(
//   (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
// );

// const last_posts = posts.slice(0, 3);
---

<BaseLayout>
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm David Ly</div>
    <div class="text-3xl py-3 font-bold">Software Engineer</div>
    <div class="py-2">
      <text class="text-lg">
        I manage this website mostly as a respository of relevant information
        pertaining to myself. Credits to
        <a class="font-bold italic" href="https://twitter.com/manuelernestog">
          @manuelernestog
        </a>
        for the template which I adapted my site from, you can navigate to the project's
        GitHub page using the button below and financially support his work
        <a class="underline" href="https://manuelernestog.github.io/paylink">
          here
        </a>
      </text>
    </div>
    <div class="mt-8">
      <a
        href="https://github.com/manuelernestog/astro-modern-personal-website"
        target="_blank"
        class="btn btn-outline ml-5"
      >
        Get This template
      </a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">
      {"<"}Portfolio Projects{"/>"}
    </div>
  </div>

  <HorizontalCard
    title="Stylized Terminal-based Portfolio"
    img="/post_img.webp"
    desc="Self-explanatory project inspired by and pulling from m4tt72's base project found here: https://github.com/m4tt72/terminal/tree/master"
    badge="WIP"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="ANSI Rendered JSON Resume/CV"
    img="/ancv_example.png"
    desc="Personalized CV (Curriculum Vitae) following the JSON resume schema [jsonresume.org] and built to be rendered based on the [ancv.io] project. Try it out for yourself by running `curl -L https://ancv.io/david-ly`"
    url="https://gist.github.com/david-ly/2add5c03b85b3e07d0d2dc9172462af1"
    badge="FOSS"
  />

  <!-- <div>
      <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
    </div>

    {
      last_posts.map((post) => (
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
        />
        <div class="divider my-0"></div>
      ))
    } -->
</BaseLayout>
